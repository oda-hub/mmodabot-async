apiVersion: v1
kind: Service
metadata:
  name: {{ include "mmoda-backend.fullname" . }}
  labels:
    {{- include "mmoda-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mmoda-backend.selectorLabels" . | nindent 4 }}
